# hackCraft2-python Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-06-22

## Active Technologies

- Python >=3.7（パッケージ `py2hackCraft2` v1.2.0） + `websocket-client>=1.6.0`（インストール環境 1.8.0）。`wss://` URL で自動TLS、`run_forever(sslopt=...)` で証明書検証制御 (001-fix-colab-wss)

## Project Structure

```text
src/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python >=3.7（パッケージ `py2hackCraft2` v1.2.0）: Follow standard conventions

## Recent Changes

- 001-fix-colab-wss: Added Python >=3.7（パッケージ `py2hackCraft2` v1.2.0） + `websocket-client>=1.6.0`（インストール環境 1.8.0）。`wss://` URL で自動TLS、`run_forever(sslopt=...)` で証明書検証制御

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
